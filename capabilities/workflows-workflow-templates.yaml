naftiko: 1.0.0-alpha2
info:
  label: Argo Workflows API — Workflow Templates
  description: 'Argo Workflows API — Workflow Templates. 5 operations. Lead operation: Argo Workflows Argo List Workflow Templates.
    Self-contained Naftiko capability covering one Argo business surface.'
  tags:
  - Argo
  - Workflow Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ARGO_API_KEY: ARGO_API_KEY
capability:
  consumes:
  - type: http
    namespace: workflows-workflow-templates
    baseUri: https://{host}:{port}/api/v1
    description: Argo Workflows API — Workflow Templates business capability. Self-contained, no shared references.
    resources:
    - name: workflow-templates-namespace
      path: /workflow-templates/{namespace}
      operations:
      - name: listworkflowtemplates
        method: GET
        description: Argo Workflows Argo List Workflow Templates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: listOptions.labelSelector
          in: query
          type: string
          description: Label selector to filter templates
      - name: createworkflowtemplate
        method: POST
        description: Argo Workflows Argo Create a Workflow Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: workflow-templates-namespace-name
      path: /workflow-templates/{namespace}/{name}
      operations:
      - name: getworkflowtemplate
        method: GET
        description: Argo Workflows Argo Get a Workflow Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateworkflowtemplate
        method: PUT
        description: Argo Workflows Argo Update a Workflow Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteworkflowtemplate
        method: DELETE
        description: Argo Workflows Argo Delete a Workflow Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ARGO_API_KEY}}'
  exposes:
  - type: rest
    namespace: workflows-workflow-templates-rest
    port: 8080
    description: REST adapter for Argo Workflows API — Workflow Templates. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/workflow-templates/{namespace}
      name: workflow-templates-namespace
      description: REST surface for workflow-templates-namespace.
      operations:
      - method: GET
        name: listworkflowtemplates
        description: Argo Workflows Argo List Workflow Templates
        call: workflows-workflow-templates.listworkflowtemplates
        with:
          listOptions.labelSelector: rest.listOptions.labelSelector
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createworkflowtemplate
        description: Argo Workflows Argo Create a Workflow Template
        call: workflows-workflow-templates.createworkflowtemplate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workflow-templates/{namespace}/{name}
      name: workflow-templates-namespace-name
      description: REST surface for workflow-templates-namespace-name.
      operations:
      - method: GET
        name: getworkflowtemplate
        description: Argo Workflows Argo Get a Workflow Template
        call: workflows-workflow-templates.getworkflowtemplate
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateworkflowtemplate
        description: Argo Workflows Argo Update a Workflow Template
        call: workflows-workflow-templates.updateworkflowtemplate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteworkflowtemplate
        description: Argo Workflows Argo Delete a Workflow Template
        call: workflows-workflow-templates.deleteworkflowtemplate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: workflows-workflow-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Argo Workflows API — Workflow Templates. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: argo-workflows-argo-list-workflow
      description: Argo Workflows Argo List Workflow Templates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: workflows-workflow-templates.listworkflowtemplates
      with:
        listOptions.labelSelector: tools.listOptions.labelSelector
      outputParameters:
      - type: object
        mapping: $.
    - name: argo-workflows-argo-create-workflow
      description: Argo Workflows Argo Create a Workflow Template
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: workflows-workflow-templates.createworkflowtemplate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: argo-workflows-argo-get-workflow
      description: Argo Workflows Argo Get a Workflow Template
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: workflows-workflow-templates.getworkflowtemplate
      outputParameters:
      - type: object
        mapping: $.
    - name: argo-workflows-argo-update-workflow
      description: Argo Workflows Argo Update a Workflow Template
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: workflows-workflow-templates.updateworkflowtemplate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: argo-workflows-argo-delete-workflow
      description: Argo Workflows Argo Delete a Workflow Template
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: workflows-workflow-templates.deleteworkflowtemplate
      outputParameters:
      - type: object
        mapping: $.
